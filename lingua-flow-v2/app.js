const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('.site-nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}))}
const panels={order:{k:'01 / WORD ORDER',t:'情報をどの順番で置くか。',d:'日本語では語尾まで待てる情報が、別の言語では早い段階で必要になることがあります。語順は単なる並び方ではなく、情報処理の順序です。'},time:{k:'02 / TENSE & ASPECT',t:'出来事を、どの時間感覚で切り取るか。',d:'「過去・現在・未来」だけでなく、完了・継続・経験など、出来事をどう捉えるかまで含めて比較すると理解が安定します。'},subject:{k:'03 / SUBJECT',t:'誰を文の中心に置くか。',d:'主語を省きやすい言語と、明示を強く求める言語では、文を作るときの出発点が変わります。'},modify:{k:'04 / MODIFICATION',t:'説明を、前に置くか後ろに置くか。',d:'名詞を説明する情報の位置や長さは、読む速度と話す組み立て方に直結します。まとまり単位で捉えるのがコツです。'},context:{k:'05 / CONTEXT',t:'どこまで言葉にし、どこから文脈に任せるか。',d:'省略や含意の許容度は言語ごとに異なります。直訳だけではなく、「何を言わなくても通じるか」まで見ると理解が深まります。'}};
const detail=document.querySelector('#map-detail');document.querySelectorAll('.map-item').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.map-item').forEach(b=>b.classList.remove('is-active'));btn.classList.add('is-active');const p=panels[btn.dataset.panel];if(detail&&p){detail.innerHTML=`<p class="eyebrow">${p.k}</p><h3>${p.t}</h3><p>${p.d}</p>`}}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();