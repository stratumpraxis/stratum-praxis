# MARKET｜Brand / Gmail / SNS Identity Routing Directive

Last Owner Update: 2026-09-08

## 0. Purpose

MARKET全体で、Brand・Gmail・SNS Account・Publishing Routeの取り違えを防ぎ、外部Distributionを止めずにRevenue Routeへ接続するためのIdentity Routing指示。

投稿数・Account数を増やすことが目的ではない。

**Market → Content → Qualified Human → Revenue Destination → Purchase / Contract / Reward → Evidence**

をBrandごとに閉じる。

---

## 1. Hard Identity Rule

### STRATUM PRAXIS

Primary Gmail:

`stratumpraxis@gmail.com`

**用途を STRATUM B2B ONLY に固定する。**

このGmailは今後、StratumのB2B営業・B2B Publishing・B2B SNS・B2B Buyer Communication・Stratum B2B Tools / Checkout / Contract関連専用とする。

以下への新規登録・SNS Recovery・Credential・Publishing Identityとして流用しない。

- Vector
- Arven
- MarketNest
- Global Work Radar
- Language / StructureFlow
- 非B2B英語圏Publishing / SNS
- その他Brand

「英語だからStratumを使う」は禁止。

---

## 2. Non-B2B English Default Identity — TEMPORARY OWNER DECISION

非B2B英語圏のPublishing / SNS展開について、当面のPrimary Identityを以下に固定する。

Brand / Working Identity:

**ORDLUME**

X account:

`@0rdlume`

Registered Gmail:

`ordlume.contact@gmail.com`

Country setting observed by Owner:

`Japan`

State:

`ACTIVE_FOR_NON_B2B_ENGLISH / TEMPORARY_PRIMARY`

この決定は「永久Brand固定」ではない。

当面、Stratum B2B以外の英語圏Publishing / SNS配信は、既存未使用Gmailを新たに割り当てる前に **ORDLUMEを第一候補として使う**。

新しいGmail / SNS Accountを作る前に、ORDLUMEで受けられるか確認する。

### ORDLUMEで扱う対象

- 非B2B英語圏Publishing
- 英語圏Creator / general audience向けDistribution
- 英語圏の非法人向け情報資産
- Market-tested English content derivatives
- 既存Revenue Destinationへ送る英語圏SNS配信

### ORDLUMEで扱わない対象

- Stratum B2B Buyer Acquisition
- Stratumの法人営業メール
- MarketNestのAI FinOps専用発信
- GWR専用Labor Intelligence Identity
- ArvenのGumroad Creator Identity
- Language / StructureFlow専用発信

---

## 3. Global X Safety Rule — HARD LOCK

**XはMARKET全体で自動運用禁止。**

対象Brandを問わず、Xへの以下を禁止する。

- API / Connector / Make / Buffer / Metricool / SimplePost / Post Bridge等による自動投稿
- 自動予約投稿
- 自動返信
- 自動DM
- 自動Follow / Unfollow
- 自動Like / Repost
- 定期Bot運用
- Agentによる無人投稿
- Fallback Routeとしての自動X投稿

Xは **MANUAL_ONLY** とする。

X Accountが存在しても、Social Publish Router / Gatewayの自動Writer候補へ入れない。

Xを使う場合はOwnerまたは明示的なHuman操作のみ。

理由：Account ban / freeze riskを避け、既存X資産を保護するため。

Xで自動実行できない場合はRevenue Route全体を止めず、Bluesky / Instagram / TikTok / Threads / Pinterest / YouTube / note / DEV / Medium / Reddit等の許可済みRouteへ切り替える。

`X blocked for automation ≠ Brand blocked ≠ Revenue Route blocked`

---

## 4. Brand Routing Map

### STRATUM

Role:

`English B2B / Buyer Acquisition / B2B Publishing`

Gmail:

`stratumpraxis@gmail.com`

SNS運用目的:

B2B Market Signal → Content → Qualified Buyer → Existing Stratum Offer → CTA → Checkout / Contract → Purchase

Xは `MANUAL_ONLY`。自動投稿・自動返信・自動DMを禁止。

---

### VECTOR

Role:

`Japanese Publishing / note / Distribution`

Primary Gmail:

`vectorpraxis.contact@gmail.com`

Known surfaces:

- note: `deft_eel6718`
- Instagram: `vect.orpraxis`
- Bluesky: `vectorpx.bsky.social`
- Pinterest: `vectorpraxis`
- TikTok: `vectorpraxismd`
- X: existing Vector account / `MANUAL_ONLY`

目的:

Research / note / Existing Asset → Native Social Content → Qualified Human → note / Revenue Destination → Purchase

---

### ARVEN / ARVEN_VOX

Role:

`Creator Distribution / Gumroad Revenue`

Known surfaces:

- TikTok: `arven_vox`
- Instagram: `arenv.ox`
- Threads: `arenv.ox`
- Bluesky: `arvenvox.bsky.social`
- Pinterest: `arven_vox`

Collision watch:

Post Bridge上に `arvenvoxxx` のTikTok表記が存在するため、同一Accountと推測しない。

Xを将来使う場合も `MANUAL_ONLY`。自動Writerへ追加しない。

目的:

Market Demand → Native Content → Human → Existing Gumroad Product → Checkout → Purchase

---

### MARKETNEST PRO

Role:

`AI Spend & Agent Economics / AI FinOps`

X:

`Do not use`

LinkedIn:

`Do not use under current policy`

Known surface:

- TikTok: `market.nest1`
- dedicated Gmail: exists, exact address must be recovered before any new account creation
- note: dedicated account exists, exact handle must be recovered before duplication

新SNSを作る前にExisting Account Registryを検索する。

目的:

AI Cost Signal → Decision Content → Qualified Company → Existing Audit / Diagnostic → Paid Engagement

---

### LANGUAGE / STRUCTUREFLOW

Role:

`Language Structure / Foreign Language Learning / Diagnostic / Paid Content`

Primary Gmail:

`language.structure.flow@gmail.com`

Known surface:

- note: `structureflow`
- X: existing account / `MANUAL_ONLY`

StructureFlowとLingua Flowは同一Brandとして扱わない。

目的:

Language Pain → Useful Explanation → Diagnostic / Existing Asset → Qualified Learner → Purchase

---

### GLOBAL WORK RADAR

Role:

`Labor Intelligence / Global Work Discovery`

Brand independence:

GWRはStratumでもVectorでもない。

`stratumpraxis@gmail.com` をGWRの日常SNS / Outreach Identityとして今後デフォルト使用しない。

専用Gmail・SNSが既に存在する可能性を前提に、再確認してから新規作成する。

GWRのX Accountが存在・発見されても `MANUAL_ONLY`。自動投稿Routeには入れない。

目的:

Labor Market Signal → Insight → Qualified Human → GWR → Search → Official Apply → Referral / Revenue

---

### ORDLUME

Role:

`Default Non-B2B English Social Identity / Temporary Primary`

Gmail:

`ordlume.contact@gmail.com`

Known surface:

- X: `@0rdlume` / `MANUAL_ONLY`

ORDLUMEのXは自動投稿に使わない。非B2B英語圏の自動Distributionを行う場合は、X以外の既存または後に明示承認されたSurfaceを使う。

---

## 5. Owned / Standby Gmail Pool

以下は保有IdentityとしてRegistryに残すが、Ownerの明示割当なしに勝手にBrand化・SNS作成・投稿元利用しない。

- `practicalaireport@gmail.com` — MOSTLY_UNUSED / STANDBY
- `agentflowopshq@gmail.com` — MOSTLY_UNUSED / STANDBY
- `qelvane.studio@gmail.com` — OWNED / STANDBY / exact use recheck
- `sovltablw@gmail.com` — OWNED / STANDBY / exact use recheck
- `language.structure.flow@gmail.com` — assigned to LANGUAGE / STRUCTUREFLOW
- `everydaygeniusjp@gmail.com` — LIFE HUCK VALT / legacy asset

これらが存在するからという理由だけで新Brandを作らない。

---

## 6. New Account Creation Gate

新しいGmail / SNS Accountを作る前に必ず以下を確認する。

1. Existing Gmail search
2. Existing SNS search
3. Existing Brand / dormant asset search
4. Existing AccountでRevenue Routeを受けられるか
5. Existing Publishing Accountと競合しないか
6. New Accountが本当に必要なEvidenceがあるか

判定:

- Existingで対応可能 → 再利用
- Identity不明 → `IDENTITY_RECHECK`
- 既存では処理不能な反復的欠損がEvidence化 → 新規作成候補

`Accountが見つからない = Accountが存在しない` と判断しない。

Xについては新規Accountを作成して自動運用を増やす方向へ進めない。

---

## 7. Social Publishing Safety

### One Account = One Primary Writer

同一SNS Accountへ複数Toolから同時書き込みしない。

Primary Writerを1つ固定し、FallbackはPrimary障害時のみ使う。

**Exception: XにはPrimary Writerを設定しない。`MANUAL_ONLY`。**

### Collision Prevention

投稿前に最低限以下を解決する。

- brand
- platform
- exact account / handle
- primary writer
- credential state
- quota state
- revenue destination

Accountを推測しない。

Platform = X の場合は自動Publish処理を実行せず `MANUAL_ONLY_X` として扱う。

### Evidence

`PUBLISHED` はPublic URLまたはPlatform Post IDが取得できた場合のみ。

Tool success / API successだけではPUBLISHED扱いしない。

View / Like / Follower / ClickはRevenueではない。

最終TruthはPurchase / Contract / Rewardの外部Source of Truth。

---

## 8. Non-B2B English Routing Rule — Effective Now

当面の非B2B英語圏Identity:

```text
English non-B2B Signal
↓
ORDLUME
@0rdlume = MANUAL_ONLY X
ordlume.contact@gmail.com
↓
X以外の許可済みSurfaceがある場合のみ自動Distribution
↓
One Existing Revenue Destination
↓
Qualified Human
↓
CTA / Checkout
↓
Purchase / Revenue Evidence
```

Stratum B2Bと混ぜない。

Vectorを英語圏へ使うか、Practical AI Report / Agent Flow Ops HQ / その他未使用Gmailを再割当するかは、今後のOwner判断まで未確定とする。

**Until changed by Owner, ORDLUME is the default non-B2B English identity, but its X account is manual-only.**

---

## 9. Final Operating Principle

Identityを増やすより、既存IdentityをRevenue Routeへ正しく接続する。

```text
Brand
↓
Correct Gmail
↓
Correct SNS Account
↓
Allowed Writer / X = MANUAL_ONLY
↓
External Action
↓
Qualified Human
↓
Revenue Destination
↓
Purchase / Contract / Reward
↓
Evidence
↓
Learning
```

Ownerを通信経路にしない。

ただしXだけは例外で、自動投稿・自動返信・自動DM等を禁止し、Human操作に限定する。

GitHub / Account Registry / shared stateをSource of Truthとして更新し、各Revenue Cellが同じIdentity Mapを参照して自律再開できる状態を維持する。
