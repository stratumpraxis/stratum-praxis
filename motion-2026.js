(()=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const root=document.documentElement;const scene=document.querySelector('.intel-scene');const parallax=[...document.querySelectorAll('.section-heading,.room-copy,.radar-ui,.connect-panel')];let tx=0,ty=0,cx=0,cy=0;addEventListener('pointermove',e=>{root.style.setProperty('--mx',`${e.clientX}px`);root.style.setProperty('--my',`${e.clientY}px`);if(scene&&innerWidth>900){const r=scene.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;if(x>-1&&x<1&&y>-1&&y<1){tx=x*1.7;ty=-y*1.25}}},{passive:true});function frame(){cx+=(tx-cx)*.045;cy+=(ty-cy)*.045;if(scene){scene.style.setProperty('--motion-rx',`${cy}deg`);scene.style.setProperty('--motion-ry',`${cx}deg`)}requestAnimationFrame(frame)}scene?.classList.add('motion-tilt');frame();const cards=[...document.querySelectorAll('.capability-card,.action-step,.solution-row,.trust-grid article,.connect-panel,.radar-ui')];cards.forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--card-x',`${e.clientX-r.left}px`);el.style.setProperty('--card-y',`${e.clientY-r.top}px`)});el.addEventListener('pointerleave',()=>{el.style.removeProperty('--card-x');el.style.removeProperty('--card-y')})});parallax.forEach((el,i)=>{el.classList.add('motion-parallax');el.dataset.motionFactor=String(.006+(i%3)*.002)});let ticking=false;function scrollMotion(){const vh=innerHeight;parallax.forEach(el=>{const r=el.getBoundingClientRect(),factor=Number(el.dataset.motionFactor||.006);const center=r.top+r.height/2-vh/2;el.style.setProperty('--motion-y',`${Math.max(-8,Math.min(8,-center*factor))}px`)});ticking=false}addEventListener('scroll',()=>{if(!ticking){ticking=true;requestAnimationFrame(scrollMotion)}},{passive:true});scrollMotion()})();