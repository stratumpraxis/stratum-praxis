(()=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const scene=document.querySelector('.intel-scene');const parallax=[...document.querySelectorAll('.manifesto-inner,.system-copy,.radar-field,.decision-inner')];let tx=0,ty=0,cx=0,cy=0;addEventListener('pointermove',e=>{if(!scene||innerWidth<900)return;const r=scene.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;if(x>-1&&x<1&&y>-1&&y<1){tx=x*.9;ty=-y*.65}},{passive:true});function frame(){cx+=(tx-cx)*.035;cy+=(ty-cy)*.035;if(scene){scene.style.setProperty('--motion-rx',`${cy}deg`);scene.style.setProperty('--motion-ry',`${cx}deg`)}requestAnimationFrame(frame)}scene?.classList.add('motion-tilt');frame();parallax.forEach((el,i)=>{el.classList.add('motion-parallax');el.dataset.motionFactor=String(.003+(i%2)*.0015)});let ticking=false;function update(){const vh=innerHeight;parallax.forEach(el=>{const r=el.getBoundingClientRect(),f=Number(el.dataset.motionFactor);const center=r.top+r.height/2-vh/2;el.style.setProperty('--motion-y',`${Math.max(-7,Math.min(7,-center*f))}px`)});ticking=false}addEventListener('scroll',()=>{if(!ticking){ticking=true;requestAnimationFrame(update)}},{passive:true});update()})();