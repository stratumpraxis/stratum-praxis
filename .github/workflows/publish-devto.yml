name: Publish Stratum Articles to DEV.to (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired publisher guard
        run: |
          echo 'RETIRED: DEV.to publishing is handled by Stratum Unified Publication Hub.'
          echo 'No article write was performed.'
